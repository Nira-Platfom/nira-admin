# ─── API connection ───────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: The admin secret key is entered by the user on the login screen.
# It is never stored in .env for the admin panel frontend.
# NEXT_PUBLIC_ variables are exposed to the browser — never put secrets here.
